part of 'shared.dart';

const double defaultMargin = 24.0;

Color primaryColor = Colors.redAccent[400];
Color primaryColorLight = Color(0xFFFDA085);
Color accentColor = Color(0xFFF6D365);

Color shimmerBaseColor = Colors.grey[300].withOpacity(0.8);
Color shimmerHighlightColor = Colors.white.withOpacity(0.5);